<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<StackPane xmlns:fx="http://javafx.com/fxml"
           fx:controller="me.niculicicris.filestore.application.controller.authentication.LoginController"
           stylesheets="@style/authentication.css">
    <VBox styleClass="card, login-card">
        <Label styleClass="title-1" alignment="CENTER">
            Welcome to FileStore
        </Label>
        <VBox alignment="CENTER" spacing="10">
            <Label>Username</Label>
            <TextField fx:id="usernameField" maxWidth="300"/>
        </VBox>
        <VBox alignment="CENTER" spacing="10">
            <Label alignment="BASELINE_LEFT">Password</Label>
            <PasswordField fx:id="passwordField" maxWidth="300"/>
        </VBox>
        <HBox alignment="CENTER" spacing="20">
            <Button onMouseClicked="#onLoginButtonClick" minWidth="80" styleClass="accent">
                Login
            </Button>
            <Button onMouseClicked="#onRegisterButtonClick" minWidth="80">
                Register
            </Button>
        </HBox>
    </VBox>
</StackPane>
